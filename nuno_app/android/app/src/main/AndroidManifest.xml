<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Required for every build type.

      Flutter's tooling injects this permission into the generated debug and
      profile manifests only, so a release APK built without it has no network
      access at all: the socket reports `xhr poll error` forever and the app
      sits on "Connecting to the server..." with nothing else to show.
      Declaring it here covers all three build types.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Lets the app tell "no connection" apart from "server is down". -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Voice chat. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:label="Nuno"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!--
              Shown while Flutter initialises, so the app does not flash a
              blank white frame on launch.
            -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!--
      Flutter's text-processing integration queries for this intent. Android 11
      and newer hide it unless it is declared here.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
